# JWT secret for OAuth state and refresh tokens. Generate:
#   openssl rand -hex 32
# Stored in things_data volume after first start.
JWT_SECRET=

# OAuth для cloud-clients (Claude.ai/Perplexity). Default off.
# Включить когда понадобится: установить true и redeploy.
OAUTH_ENABLED=false
